import { createMuiTheme } from "@material-ui/core/styles";

export const theme = createMuiTheme({
  palette: {
    common: {
      black: "#121629",
      white: "#fffffe"
    },
    background: {
      default: "#232946"
    },
    primary: {
      main: "#eebbc3"
    },
    secondary: {
      main: "#b8c1ec"
    },
    text: {
      primary: "#121629",
      secondary: "#b8c1ec"
    }
  }
});
